	程序根据什么特征来区分调用哪个重载函数?-只能靠参数而不能靠返回值类型的不同来区分重载函数。编译器根据参数为每个重载函数产生不同的内部标识符
重载函数的参数至少要有一方面不同，表现如下：
	1.函数的参数类型和个数不同；
	2.函数参数的顺序不同（要求）
函数重载是指在同一作用域内，可以有一组具有相同函数名，不同参数列表的函数，这组函数被称为重载函数。重载函数通常用来命名一组功能相似的函数，这样做减少了函数名的数量，避免了名字空间的污染，对于程序的可读性有很大的好处。
要求:类的构造函数跟类名相同;函数名+参数列表，因为函数重载取决于参数的类型、个数，而跟返回类型无关
函数声明告诉编译器函数的名称、返回类型和参数，在 C++ 中，函数由一个函数头和一个函数主体组成——函数声明
	第一个错误是引号的使用，一开始的是中文输入的引号，程序应该用的是英文的双引号。第一个错误是输出的{0}与{1}括号中间存在空格，而标准的正确形式是不能有空格存在，将其删除即可
	3.用到了while循环 是在循环开始前判断是否满足条件进行循环（通过在循环前添加初始化语句可以达到for循环效果）还用到了switch语句，在一个 switch 中可以有任意数量的 case 语句。每个 case 后跟一个要比较的值和一个冒号。当遇到?break?语句时，switch 终止，控制流将跳转到 switch 语句后的下一行。不是每一个 case 都需要包含?break。如果 case 语句不包含?break，控制流将会?继续?后续的 case，直到遇到 break 为止。
	4.将键盘输入的数字字符串表示形式转化为它等值的32位有效有符号整数
	4.3使用函数重载的作用是对函数的实参参数实行初始化。第二个是使函数的定义和调用更具有一般性，一般功能相同的函数我们可以使用相同的函数，减低编程的复杂性，大大简化代码的阅读性。规定是要求重载函数的参数个数不同和参数类型不同。
		标点符号的使用错误，使用了中文输入法下的标点符号，程序无法运行；解决方法是改用了英文输入法下的符号便可以解决
	一开始编写的时候忘记了头文件，导致程序无法正常运行，解决方法是将头文件加入便可以解决漏掉了using namespace System这一句话，是系统命名空间，是?.Net framework,?的一部分，?"CLR" projects的必备。解决方法是加入using namespace System在头文件下方便可以正常运行代码
	6.1学了函数的定义、函数体以及函数调用，明白理解了形参与实参的区别。学了类型转换语句，明白了Parse关键字。学了函数重载，明白了函数重载的重要性以及重载的条件
	6.2实验操作的总结：明白了CLR控制台程序，CLR是公共语言运行库；以及掌握了如何新建一个新的控制台程序
	6.3程序调试的总结：程序调试是为了尽早地发现存在于程序的错误，所以应该采用良好的编程风格；进行代码复查；
	6.4投入时间以及效率分析：投入的时间除了上课时间都选择在晚上的课余时间进行学习，通过观看老师的教学视频，不断实践，效率较高
	6.5心得与体会：开始学习c++时候，发现c与c++有着许多异曲同工之处，学好c语言让我们学习c++更加简单，虽然c++有着一定的学习难度，但是这对我们未来的学习与工作都有着很大的帮助，它是面向对象的一种编程语言，对学习Java等有着很大的帮助。希望在老师的指导下，通过自己的不断努力，真正得学好用好c++编程语言。

	